NGINX_ACCESS  "%{HTTPDATE:time_local}" %{IPORHOST:remote_addr} "(?:%{WORD:http_method} %{URIPATHPARAM:request_url}(?: HTTP/%{NUMBER:http_version})?|-)" %{NUMBER:request_length} %{INT:status} %{INT:bytes_sent} %{INT:body_bytes_sent} %{HOSTPORT:http_referer} %{QS:http_user_agent} "%{HOSTPORT:upstream_addr}" "%{INT:upstream_status}" "%{NUMBER:upstream_connect_time}" "%{NUMBER:upstream_response_length}" "%{NUMBER:request_time}" "%{NUMBER:upstream_response_time}" "%{NUMBER:upstream_header_time}" %{WORD:nginx_version}
