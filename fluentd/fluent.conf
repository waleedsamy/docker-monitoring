<source>
      @type tail
      format nginx
      tag nginx.access
      path /var/log/nginx/access*.log
      exclude_path ["/var/log/nginx/*.gz"]
      format none
      message_key log_entry
  </source>

  <match nginx.access>
    @type stdout
  </match>
